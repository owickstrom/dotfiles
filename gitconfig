[color]
	ui = auto
[core]
	editor = vim
	excludesfile = ~/.gitignore
[user]
	name = Oskar Wickström
	email = oskar.wickstrom@live.com
[alias]
  co = checkout
  ci = commit
  st = status
  br = branch
  hist = log --pretty=format:\"%C(yellow)%h%C(reset) %C(green)%ad%C(reset) | %s%C(magenta)%d%C(reset) %C(cyan)[%an]%C(reset)\" --graph --date=short
  type = cat-file -t
  dump = cat-file -p
