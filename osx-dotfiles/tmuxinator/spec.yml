name: spec
root: ~/Projects/purescript

windows:
  - spec:
      root: ~/Projects/purescript/purescript-spec
      panes:
        - vim
        - pulp -w test
  - xunit:
      root: ~/Projects/purescript/purescript-spec-reporter-xunit
      panes:
        - vim
        - pulp -w test
  - quickcheck:
      root: ~/Projects/purescript/purescript-spec-quickcheck
      panes:
        - vim
        - pulp -w test
